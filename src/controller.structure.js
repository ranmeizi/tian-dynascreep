const worker = require('./creep.worker')
const energyKeeper = require('./creep.energyKeeper')

/**
 * 
 * @param {Structure} structure 
 */
function controller(structure) {
    switch (structure.structureType) {
        default:
            // ???
            break;
    }
}

module.exports = controller