
/** @type {Handler<StructureSpawn>} */
const handler = {
    prepare(spawn) {
        // 检查状态


    },
    run(spawn) {
        // 检查行动队列
        const action = spawn.memory.actions[0]
    },
    abilitys: {
        spawn() {

        },
        recycle() {

        },
        renew() {

        }
    }
}